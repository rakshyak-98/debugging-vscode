{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "runFlask",
            "type": "shell",
            "command": "${workspaceFolder}/.env/bin/flask", // or path to flask in your virtual environment
            "args": [
                "run"
            ],
            "group": "none",
            "problemMatcher": []
        }
    ]
}